Right after starting shell, the following has to be run:
R CMD javareconf -e
this creates an R shell with support for the current version of Java.

To start R for interactive experiments,
R --no-save

